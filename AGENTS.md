# Agent rules

## .cs rules

- after changing .cs files always run `csharpier format .` in the repo root

## .csproj rules

- after changing .csproj files always run `csharpier format .` in the repo root
