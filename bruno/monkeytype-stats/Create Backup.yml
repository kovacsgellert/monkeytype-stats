info:
  name: Create Backup
  type: http
  seq: 1

http:
  method: POST
  url: https://localhost:5001/api/backup
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
